version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dexra_backend
    ports:
      - "9000:9000"
    env_file:
      - .env.prod
    restart: unless-stopped
